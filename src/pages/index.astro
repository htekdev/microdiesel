---
import BaseLayout from '~/layouts/BaseLayout.astro';
import Hero from '~/components/Hero.astro';
import StatBar from '~/components/StatBar.astro';
import ServicesGrid from '~/components/ServicesGrid.astro';
import FleetSection from '~/components/FleetSection.astro';
import AboutSection from '~/components/AboutSection.astro';
import ContactSection from '~/components/ContactSection.astro';
import { site } from '~/data/site';

const orgSchema = {
  '@context': 'https://schema.org',
  '@type': ['AutoRepair', 'LocalBusiness'],
  '@id': `${site.url}#organization`,
  name: site.legalName,
  alternateName: site.short,
  url: site.url,
  logo: `${site.url}/logo-mj-merida.jpg`,
  image: `${site.url}/logo-mj-merida.jpg`,
  description: 'Taller diésel en Mérida, Yucatán. Servicios preventivos y correctivos para todo tipo de unidades diésel — motor, suspensión y frenos. Atendemos a particulares y a flotillas de 3 a 30+ vehículos. Más de 35 años de experiencia.',
  telephone: site.phoneE164,
  email: site.email,
  founder: { '@type': 'Person', name: site.owner },
  foundingDate: '2013',
  address: {
    '@type': 'PostalAddress',
    streetAddress: site.address.street,
    addressLocality: site.address.city,
    addressRegion: site.address.region,
    postalCode: site.address.postalCode,
    addressCountry: site.address.country,
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: site.geo.latitude,
    longitude: site.geo.longitude,
  },
  openingHours: site.hoursSchema,
  areaServed: [
    { '@type': 'City', name: 'Mérida' },
    { '@type': 'State', name: 'Yucatán' },
  ],
  priceRange: '$$',
  slogan: site.tagline,
  knowsAbout: [
    'Motor diésel',
    'Mecánica general diésel',
    'Servicios preventivos',
    'Servicios correctivos',
    'Reparación de motor',
    'Suspensión',
    'Frenos',
    'Mantenimiento de flotillas',
  ],
};

const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  '@id': `${site.url}#website`,
  url: site.url,
  name: site.brand,
  publisher: { '@id': `${site.url}#organization` },
  inLanguage: 'es-MX',
};
---
<BaseLayout
  title={`${site.brand} | Taller diésel y flotillas en Mérida, Yucatán`}
  description="Micro Diesel de Mérida — taller diésel en Mérida, Yucatán. Servicios preventivos y correctivos para todo tipo de unidades diésel: motor, suspensión y frenos. Atendemos a particulares y a flotillas de 3 a 30+ vehículos. +35 años de experiencia."
  schema={[orgSchema, websiteSchema]}
>
  <Hero />
  <StatBar />
  <ServicesGrid />
  <FleetSection />
  <AboutSection />
  <ContactSection />
</BaseLayout>
